# Spotify API credentials — create an app at https://developer.spotify.com/dashboard
# (Client Credentials flow; no redirect URI needed.)
# Leave blank to set them later from the web UI's settings page instead.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
